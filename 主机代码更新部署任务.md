# 任务：OneDeal 主机代码更新部署（git 拉取 → 构建 → 重启，数据库零影响）

> 交给部署在主机（mk-desktop，Ubuntu 24.04，/opt/onedeal）上的 agent 执行。
> 本文件可以直接发给 agent，也可以让 agent 读取本文件后执行。
> 每次 Windows 开发机发布新版本后，用本任务把代码更新到主机。

## 仓库结构（2026-08-22 变更，重要）

| 仓库 | 地址 | 内容 | 谁拉 |
|---|---|---|---|
| **私有（开发主线）** | `https://github.com/csyccc111/OneDeal-personal.git` | 完整代码 + 内部文档（PROJECT_MEMORY/部署指南等） | **主机从这里拉** |
| 公开 | `https://github.com/csyccc111/OneDeal.git` | 纯净代码快照（单提交、无内部文档） | 展示用，主机不用 |

**旧 gitee 远程（csy11453/onedeal）已删除**，主机 remote 必须改为上面的私有仓库。

## 背景

- 部署目录 `/opt/onedeal`（systemd 服务 onedeal，运行用户 onedeal，Node 22）
- 数据库：`/opt/onedeal/.next/standalone/data/onedeal.db`；附件 `standalone/uploads/`；账号密码 `standalone/data/password.json`
- 更新流程：`git pull` → `sudo bash deploy/deploy.sh`（自动装依赖/构建/应用迁移/重启，幂等）

## 操作步骤（严格按顺序）

### 0. 连通性检查（先做，别跳过）

> ⚠️ **权限约定（重要）**：/opt/onedeal 属主是 `onedeal` 用户。当前终端用户可能是 `mk` 或 root：
> 1. 先执行 `ls -ld /opt/onedeal/.git` 和 `whoami` 确认属主
> 2. 属主是 onedeal → 本任务所有 git 命令必须用 **`sudo -u onedeal git ...`**（普通用户执行会报"权限不够"，sudo 直接执行会报 dubious ownership）
> 3. 命令执行失败一律先用这个身份规则排查，不要反复换用户重试

```bash
# 确认 remote 指向私有仓库（旧 gitee 已废弃）
sudo -u onedeal git remote -v
# 若还是 gitee 地址，改之：
sudo -u onedeal git remote set-url origin https://github.com/csyccc111/OneDeal-personal.git

# 测试能否访问 GitHub 私有仓库
sudo -u onedeal git ls-remote origin HEAD
```

- **直接显示 HEAD 哈希** → 认证已配好，继续第 1 步
- **提示 `Username for 'https://github.com'`** → 私有仓库需要认证，走**分支 A**（SSH key）
- **报 TLS/SSL/Connection reset 错误** → 直连 GitHub 被墙，走**分支 B**（SSH over 443）

**分支 A / 分支 B 都需要用户在 GitHub 网页上加公钥，agent 无法代办**——执行到"输出公钥"后**停下等用户**，不要自行继续。

```bash
# 分支 A：HTTPS 认证（SSH over 22 可用时）
sudo -u onedeal ssh-keygen -t ed25519 -C "onedeal-host" -N "" -f ~/.ssh/id_ed25519
sudo -u onedeal cat ~/.ssh/id_ed25519.pub     # ← 输出这行，等用户加到 GitHub 后再继续
# 用户加完公钥后：
sudo -u onedeal git remote set-url origin git@github.com:csyccc111/OneDeal-personal.git
sudo -u onedeal git ls-remote origin HEAD     # 应直接显示哈希

# 分支 B：SSH over 443（22 端口被墙时用）
sudo -u onedeal ssh-keygen -t ed25519 -C "onedeal-host" -N "" -f ~/.ssh/id_ed25519
sudo -u onedeal cat ~/.ssh/id_ed25519.pub     # ← 输出这行，等用户加到 GitHub 后再继续
# 用户加完公钥后：
sudo -u onedeal git remote set-url origin ssh://git@ssh.github.com:443/csyccc111/OneDeal-personal.git
sudo -u onedeal git ls-remote origin HEAD     # 应直接显示哈希
```

> 公钥添加到：GitHub → Settings → SSH and GPG keys → New SSH key（用户操作，粘贴整行 `ssh-ed25519 AAAA... onedeal-host`）

### 1. 配置远程仓库（首次执行需要；之后确认即可）

```bash
cd /opt/onedeal
sudo -u onedeal git remote -v    # 查看当前 remote（第 0 步已处理过则跳过）
# 若 origin 不是私有仓库地址，改之：
sudo -u onedeal git remote set-url origin https://github.com/csyccc111/OneDeal-personal.git
```

### 2. 检查本地状态（重要：主机不应有未提交改动）

```bash
sudo -u onedeal git status --short
```

- **无输出** → 继续
- **有输出** → 逐条判断：
  - 若是"已跟踪文件的修改/删除"（如历史任务遗留的手工改动）→ **先备份**（`cp -r <文件> /tmp/`）再 `git stash` 或 `git checkout -- .`，并在汇报里列出
  - 若是未跟踪文件（如新任务文档）→ 保留即可（reset 不影响未跟踪文件），汇报里列出
  - 拿不准 → 停止报告

### 3. 备份数据库（必须，先备份）

```bash
cp /opt/onedeal/.next/standalone/data/onedeal.db /tmp/onedeal.db.bak.$(date +%Y%m%d%H%M%S)
```

### 4. 拉取最新代码

```bash
sudo -u onedeal git fetch origin
sudo -u onedeal git reset --hard origin/master
sudo -u onedeal git log --oneline -3    # 记录新版本提交号（汇报用）
```

### 5. 判断是否有数据库结构变更

```bash
# 对比本次拉取前后的迁移目录（有新增目录 = schema 变了）
ls -d prisma/migrations/*/ | sort | tail -5
```

- 无新增迁移 → migrate deploy 无操作，数据库零影响（大部分发版属于此类）
- 有新增迁移 → deploy.sh 第 7 步会自动应用；**应用前确认第 3 步备份已完成**；若担心，先手动验证：
  `sudo -u onedeal npx prisma migrate status`

### 6. 构建部署（⚠️ 只能跑 deploy.sh，禁止手敲 npm run build）

```bash
sudo bash deploy/deploy.sh
```

deploy.sh 行为说明（数据库零影响的保障）：
- 第 3 步 rsync 同步代码时**排除** `node_modules/.next/data/uploads/.git` → 数据库/附件/.env/密码文件全部不动
- 第 4 步 `.env` 已存在则保留（不覆盖 DATABASE_URL/AUTH_SECRET）
- 第 7 步 `prisma migrate deploy` 只应用新增迁移，不 reset 不删数据
- 第 8 步重启 systemd 服务
- 全程约几分钟，看到 `===== 部署完成 =====` 即成功

### 7. 验证

```bash
systemctl status onedeal --no-pager | head -6      # active (running)
curl -sI http://127.0.0.1:3000 | head -3           # HTTP/1.1 200 或 302
# 登录页可达：
curl -s http://127.0.0.1:3000/login | grep -o "登录" | head -1
# 确认数据库数据还在（订单/客户数量不变）：
node -e "const D=require('better-sqlite3');const db=new D('/opt/onedeal/.next/standalone/data/onedeal.db');console.log('订单:',db.prepare('SELECT COUNT(*) n FROM \"Order\"').get().n,'客户:',db.prepare('SELECT COUNT(*) n FROM Customer').get().n);db.close();"
# 新功能抽查：按本次发版说明验证（如打印页列宽/通知铃铛/WxPusher 设置页）
```

## 注意事项（红线）

1. **禁止在主机执行 `npm run build`**（会清掉 standalone/data 目录导致数据库打不开）——部署只准 `sudo bash deploy/deploy.sh`。
2. **不要动**：frpc/frps/花生壳（隧道相关）、`/opt/frp/`、ECS；`onedeal.service` 由 deploy.sh 管理，不要手改。
3. **主机只拉取，不提交**：不要 `git commit`/`git push` 任何东西；本地出现改动先报告。
4. 数据库操作前必须备份（第 3 步）；`reset --hard` 会丢弃本地已跟踪文件的改动（第 2 步已处理）。
5. 与任务描述不符、命令报错 → **停止并贴完整错误**，不要反复重试不同命令。
6. 部署期间业务中断约 1-3 分钟（构建完成后重启），告知用户可接受再执行。

## 完成汇报格式

1. 连通性/remote 检查结果
2. 本地状态检查结果（有无改动、如何处理）
3. 新版本提交号 + 本次变更摘要（git log 内容）
4. 迁移判断（有无新增）
5. deploy.sh 输出尾部（部署完成标志）
6. 验证结果（服务状态/登录页/订单客户数/新功能抽查）
7. 遗留问题（如有）

---

记录者：DeepSeek-V4 Flash（agent: DeepSeek Harness）| 2026-08-22
